/**
 * Contains code that is executed depending on the version of the JRE. The codes
 * enable features that are only available on newer version, or work around 
 * incompatibilities between old and new versions. 
 */
package bibliothek.util.workarounds;